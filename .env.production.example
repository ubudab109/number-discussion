# Production Environment Variables
# Copy this to .env.production and fill in your values

# Database Configuration (Custom ports to avoid conflicts)
DB_NAME=number_discussion_prod
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=postgres
DB_PORT=5432

# Backend Configuration
PORT=3001
JWT_SECRET=your_very_secure_jwt_secret_here_change_this

# Frontend Configuration
VITE_API_URL=https://your-domain.com/api

# Docker Ports (External ports on VPS)
POSTGRES_EXTERNAL_PORT=5433
BACKEND_EXTERNAL_PORT=3001
FRONTEND_EXTERNAL_PORT=8081
